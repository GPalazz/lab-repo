name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        -name: cat swapi.html

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Display a message
        run: echo "Hello, GitHub Actions!
